services:
  - type: web
    name: agentcast
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PUSH_TOKEN
        generateValue: true
